// Q1. Write a c program to calculate the area of a rectangle:

// a) using hardcoded inputs &

#include <stdio.h>

int main()
{
    int length = 3, breadth = 5;
    int area = length * breadth;
    printf("the area of this rectangel is %d", area);
    return 0;
}