#include <stdio.h>
#include <string.h>
#include <string.h>
int main()
{
    char st1[30] = "hellow";
    char *st2 = "shanto";
    strcat(st1, st2);
    printf("now the st1 is %s", st1);
    return 0;
}
